<div class="ui grid">
  <div class="three column row">
    <div class="column"></div>
    <div class="column">
        <div class="ui segment">
        <h1>ようこそ</h1>
        <p>トップページです。</p>
        
        
        <% if user_signed_in? %>
          <% @users.each do |user| %>
           <%= user.email %>
           <%= user.name %>
          <% end %>
        <% end %>
        </div>

    </div>
    <div class="column"></div>
  </div>
</div>